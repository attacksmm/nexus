{
  "id": "customer-db",
  "name": "База клиентов",
  "version": "2.1.3",
  "description": "Мульти-таблица клиентов: id, platform_id, custom_fields (JSON). Полный CRUD REST API.",
  "env_vars": {
    "NEXUS_CUSTOMER_DB_API_TOKEN": "Bearer token для внешних upsert/batch-upsert запросов. Если не задан, используется NEXUS_OPENROUTER_API_TOKEN."
  }
}
